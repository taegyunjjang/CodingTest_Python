#include <bits/stdc++.h>
#include <iostream>

#define endl '\n'
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string str;
    
    int i = 0;
    while (i < 100){ 
        getline(cin, str);        
        cout << str << endl;
        i += 1;
    }
}
